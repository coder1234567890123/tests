node_modules/

# Local environment variables should not be committed.
# A generic .env (copied from .env.example and populated with non-sensitive defaults)
# can be committed if desired. Specific local overrides should use .env.local.
.env.local
.env.*.local
